# Configurações do Google Gemini
GEMINI_API_KEY=sua_chave_api_aqui
GEMINI_MODEL=gemini-1.5-flash
GEMINI_TEMPERATURE=0.1

# Configurações do BigQuery (opcional)
# Se não configurar, apenas o Excel será gerado
BIGQUERY_ENABLED=false

# OPÇÃO 1 (Simples): Use gcloud CLI para autenticar
# Execute: gcloud auth application-default login
# Neste caso, só precisa habilitar o BigQuery acima (true)
# Os valores padrão já estão configurados: rj-cvl.adm_contrato_gestao.despesas

# OPÇÃO 2 (Manual): Configure manualmente (opcional - só se quiser sobrescrever os padrões)
# BIGQUERY_PROJECT_ID=rj-cvl
# BIGQUERY_DATASET=adm_contrato_gestao
# BIGQUERY_TABLE=despesas

# OPÇÃO 3 (Service Account): Use arquivo de credenciais (opcional)
# GOOGLE_APPLICATION_CREDENTIALS=/caminho/para/service-account.json
