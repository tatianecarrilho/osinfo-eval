# ============================================================================
# CREDENCIAIS (OBRIGATÓRIO)
# ============================================================================

# Chave da API do Google Gemini (OBRIGATÓRIO)
# Obtenha em: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=sua_chave_api_aqui

# ============================================================================
# BIGQUERY - CREDENCIAIS (OBRIGATÓRIO se BIGQUERY estiver habilitado)
# ============================================================================

# Caminho do arquivo de credenciais do BigQuery
# Coloque o arquivo JSON de service account na raiz do projeto
BIGQUERY_CREDENTIALS_PATH=rj-nf-agent-tati.json

# ============================================================================
# CONFIGURAÇÕES OPCIONAIS
# ============================================================================
# As configurações abaixo são opcionais. Se não forem definidas aqui,
# os valores do arquivo config.py serão usados.
# ============================================================================

# Sobrescrever modelo do Gemini (opcional - padrão: gemini-1.5-flash)
# GEMINI_MODEL=gemini-1.5-pro

# Sobrescrever temperatura do Gemini (opcional - padrão: 0.1)
# GEMINI_TEMPERATURE=0.1

# Desabilitar BigQuery (opcional - padrão: true)
# BIGQUERY_ENABLED=false

# Sobrescrever configurações do BigQuery (opcional - padrões em config.py)
# BIGQUERY_PROJECT_ID=rj-nf-agent
# BIGQUERY_DATASET=poc_osinfo_ia
# BIGQUERY_TABLE=despesas_recorte
